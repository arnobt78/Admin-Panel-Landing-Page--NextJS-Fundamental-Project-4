/** Type declaration for @fullcalendar/react default export (used in calendar scene). */
declare module "@fullcalendar/react" {
  import type { CalendarOptions } from "@fullcalendar/core";

  const FullCalendar: React.ComponentType<CalendarOptions>;
  export default FullCalendar;
}
