/** Route: /calendar – FullCalendar with day/week/month/list views; add/delete events via dialogs. */
import Calendar from "@/scenes/calendar/calendar";

export default function CalendarPage() {
  return <Calendar />;
}
